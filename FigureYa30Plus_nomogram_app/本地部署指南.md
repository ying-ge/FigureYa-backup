# 本地部署指南 - Nomogram App

## 🚀 快速开始

### 方法1: 使用Python HTTP服务器（推荐）

```bash
# 进入应用目录
cd /Users/mypro/Downloads/FigureYa/FigureYa30Plus_nomogram_app

# 启动服务器
python3 -m http.server 8000

# 打开浏览器访问
# 桌面浏览器: http://localhost:8000
# 手机浏览器: http://你的电脑IP:8000
```

### 方法2: 使用Node.js serve

```bash
# 进入应用目录
cd /Users/mypro/Downloads/FigureYa/FigureYa30Plus_nomogram_app

# 安装serve（如果还没安装）
npm install -g serve

# 启动服务器
serve -l 8000

# 打开浏览器访问
# http://localhost:8000
```

### 方法3: 使用PHP内置服务器

```bash
# 进入应用目录
cd /Users/mypro/Downloads/FigureYa/FigureYa30Plus_nomogram_app

# 启动服务器
php -S localhost:8000

# 打开浏览器访问
# http://localhost:8000
```

## 📱 在手机上访问

### 1. 确保手机和电脑在同一WiFi网络

### 2. 找到电脑的IP地址

**Mac:**
```bash
ifconfig | grep "inet " | grep -v 127.0.0.1
```

**Windows:**
```cmd
ipconfig
```

**Linux:**
```bash
ip addr show
```

### 3. 在手机浏览器访问

假设电脑IP是 `192.168.1.100`，端口是 `8000`

- 在手机浏览器中输入: `http://192.168.1.100:8000`
- iPhone用Safari浏览器
- Android用Chrome浏览器

### 4. 安装到主屏幕

**iPhone:**
1. Safari浏览器打开应用
2. 点击底部"分享"按钮
3. 选择"添加到主屏幕"
4. 点击"添加"

**Android:**
1. Chrome浏览器打开应用
2. 点击右上角菜单
3. 选择"添加到主屏幕"
4. 点击"安装"

## 🧪 测试功能

### 1. 测试基本功能
```
1. 打开应用
2. 输入测试数据：
   - 年龄: 50
   - 性别: 女性
   - 胆红素: 2.5
   - 铜: 100
   - 疾病分期: 2
   - 治疗方案: 对照组
3. 点击"计算生存率"
4. 查看结果是否正常显示
```

### 2. 测试CSV导出
```
1. 计算完成后，向下滚动
2. 点击"💾 导出当前记录"
3. 检查是否下载了CSV文件
4. 用Excel或Numbers打开CSV文件
5. 验证中文是否正常显示
```

### 3. 测试历史记录
```
1. 输入不同的数据，计算3-5次
2. 观察"已保存 X 条记录"数字增加
3. 点击"📥 导出所有历史记录"
4. 检查CSV文件是否包含所有记录
5. 点击"🗑️ 清空历史"
6. 确认记录数归零
```

### 4. 测试离线功能
```
1. 正常访问应用一次（让PWA缓存）
2. 断开网络连接
3. 刷新页面
4. 验证应用仍可正常使用
5. 测试计算和导出功能
```

## 🔧 故障排除

### 问题1: 无法访问应用

**解决方法:**
- 检查服务器是否正常运行
- 检查防火墙是否阻止端口
- 尝试使用不同的端口（如8080、3000）
- 确保URL正确（包含 `http://`）

### 问题2: 手机无法访问

**解决方法:**
- 确保手机和电脑在同一WiFi
- 检查电脑IP地址是否正确
- 关闭电脑的防火墙测试
- 尝试用电脑IP的不同端口

### 问题3: CSV导出不工作

**解决方法:**
- 检查浏览器的下载权限
- 查看浏览器的下载文件夹设置
- 尝试在其他浏览器中测试
- 检查浏览器控制台是否有错误

### 问题4: 中文乱码

**解决方法:**
- 用Excel的"数据"->"获取数据"导入CSV
- 选择UTF-8编码
- 或使用Google Sheets、Numbers等自动识别UTF-8的软件

### 问题5: 离线不工作

**解决方法:**
- 清除浏览器缓存
- 重新访问应用（让Service Worker重新安装）
- 检查浏览器是否支持PWA（Safari 11.1+, Chrome 40+）
- 必须通过HTTP服务器访问（不能直接打开HTML文件）

## 📊 数据存储说明

### localStorage限制
- 大多数浏览器限制: 5-10MB
- 每条记录约: 0.5KB
- 可存储约: 10,000-20,000条记录

### 清除数据
```javascript
// 在浏览器控制台执行
localStorage.removeItem('nomogram_history');
```

### 查看数据
```javascript
// 在浏览器控制台执行
console.log(localStorage.getItem('nomogram_history'));
```

## 🌐 公网部署（可选）

如果需要在外网访问，可以考虑：

### 选项1: GitHub Pages
1. 创建GitHub仓库
2. 上传应用文件
3. 启用GitHub Pages
4. 通过 `https://username.github.io/repo-name` 访问

### 选项2: Netlify/Vercel
1. 注册Netlify或Vercel账号
2. 拖拽应用文件夹上传
3. 自动部署并获得HTTPS链接

### 选项3: 内网穿透（临时使用）
```bash
# 使用ngrok
ngrok http 8000

# 获得临时的公网URL
# https://xxxxx.ngrok.io
```

## 📝 开发调试

### 启用开发者工具
- Chrome/Edge: F12 或 Ctrl+Shift+I
- Safari: 需先在偏好设置中启用"开发"菜单
- Firefox: F12 或 Ctrl+Shift+I

### 查看Service Worker
- Chrome: chrome://serviceworker-internals/
- Firefox: about:debugging#/runtime/this-firefox
- Safari: 开发 > Service Workers

### 清除缓存重新测试
```javascript
// 在浏览器控制台执行
navigator.serviceWorker.getRegistrations().then(function(registrations) {
  for(let registration of registrations) {
    registration.unregister();
  }
});
caches.keys().then(function(names) {
  for (let name of names) caches.delete(name);
});
```

## ✅ 部署检查清单

- [ ] 服务器正常运行
- [ ] 桌面浏览器可以访问
- [ ] 手机浏览器可以访问
- [ ] 可以进行计算
- [ ] CSV导出功能正常
- [ ] 历史记录保存正常
- [ ] 离线模式可用
- [ ] PWA可以安装到主屏幕
- [ ] 中文显示正常
- [ ] 在多种设备上测试通过

---

**注意**: 此应用完全在本地运行，不需要配置数据库或后端服务器。

